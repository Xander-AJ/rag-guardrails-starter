# Providers

Human-facing guide to the pluggable LLM backends in rag-guardrails-starter.

Covers the base provider contract and the bundled backends (OpenAI,
OpenRouter, LangChain), plus how to add a new provider.

> Content to be written.
