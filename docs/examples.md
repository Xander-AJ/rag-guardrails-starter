# Examples

Guide to the runnable scripts in `examples/`:

- `basic_rag.py` — minimal retrieve -> generate flow.
- `with_guardrails.py` — full guardrail stack around the pipeline.
- `healthcare_domain.py` — domain-specific (healthcare) configuration.
- `custom_provider.py` — plugging in a custom LLM backend.

> Content to be written.
